define([
    'src/hijax'
],
function(Hijax) {
    var heist = Hijax.proxy('home', '/');

    describe('Heist proxying tests', function() {
        it('passes', function(done) {
            done();
        });
    });
});
